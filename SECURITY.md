# Security

This skill contains instructions and references only. It does not require credentials,
network access, install hooks, or executable bundled scripts.

Before reporting a security concern, verify that it is caused by this repository rather
than by the agent client, profiler, framework, model code, or command the user supplied.
For sensitive reports, contact the maintainer privately through the security-reporting
mechanism configured on the GitHub repository.
